:ruby
  product_name = @product.name
  product_issues_url = "/products/#{@product.id}/issues"

- htitle "创建问题或建议: #{product_name}"

- hbreadcrumb '首页', '/'
- hbreadcrumb @product.name, "/products/#{@product.id}"
- hbreadcrumb '问题或建议', "/products/#{@product.id}/issues"
- hbreadcrumb '新建问题或建议', "/products/#{@product.id}/issues/new"

.page-new-issue
  =flash_info
  =form_for [@product, @issue], :html => { :multipart => true, :method => 'post' } do |f|
    .field.content.need
      %label 请在下面填写你的问题或建议描述
      =f.text_area :content

    .field.items
      .item-head
        .f.name 图片
      .item-list
        .item
          .f.name
            %input{:type=>'file', :name=>'issue[model_attaches_attributes][][image]'}
          
          .f.ops
            %a.delete-issue-attach{:href=>'javascript:;'} 减少一项
            
      %a.add-issue-attach{:href=>'javascript:;'} 增加一项



    .field.submit
      %a.form-submit-button{:href=>'javascript:;'} 确定
      %a.form-cancel-button{:href=>'javascript:history.go(-1);'} 取消