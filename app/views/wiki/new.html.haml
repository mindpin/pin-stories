

.page-title 创建 WIKI 词条

.page-new-wiki-page
  = flash[:error]
  = form_for @wiki_page, :url => { :controller => '/wiki', :action => "create" } do |f|
    .field
      %label.need 页面标题
      = f.text_field :title
      = f.hidden_field :product_id, :value => params[:product_id]
      
    .field
      %label.need 内容
      %textarea{:name=>'wiki_page[content]'}
      
    .field
      %a.form-submit-button{:href=>'javascript:;'} 保存
      %a.form-preview{:href=>'javascript:;'} 查看预览
      %a.form-cancel{:href=>'javascript:;'} 取消
