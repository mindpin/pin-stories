-# MINDPIN 工程原型Layout

:ruby
  tstr = ((yield :title) || '').gsub("\n",'')
  title = tstr.blank? ? 'MINDPIN - 敏捷开发管理' : "#{tstr} - MINDPIN - 敏捷开发管理"

!!! 5
%html{:lang=>'zh'}
  %head
    %meta{:charset=>'utf-8'}
    %title=title
    = csrf_meta_tags
    = stylesheet_link_tag 'application'
    = yield :css
    = yield :head

  %body.echo.gray
    .page-wrap
      .page-container
        .page-header
          .h
            .title=title
            -if logged_in?
              .user
                .current-user 欢迎你，#{current_user.name}
                .logout=link_to '登出','/logout'
        .page-content
          = yield

    = javascript_include_tag 'application'
    = yield :javascript