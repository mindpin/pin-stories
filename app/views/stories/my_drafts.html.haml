.page-title Stories


.page-wiki-search-results

  - @my_drafts.each do |draft|
    :ruby
      drafted_hash = draft.drafted_hash
      drafted_hash = Marshal.load(drafted_hash)
 

    .wiki-page
      .ct=link_to drafted_hash[:how_to_demo], "/stories/show_draft?temp_id=#{draft.temp_id}"
      .ct=link_to drafted_hash[:tips], "/stories/show_draft?temp_id=#{draft.temp_id}"

  .paginate=will_paginate @my_drafts